services:
  - type: web
    name: aimedu-platform
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 seed_data.py && gunicorn run:app --bind 0.0.0.0:$PORT
